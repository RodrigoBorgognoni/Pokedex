export default class Pokemon { 
    constructor(order, name, color, types, photo) 
    { 
        this.order = order; 
        this.name = name; 
        this.color = color; 
        this.types = types; 
        this.photo = photo;
    }
}